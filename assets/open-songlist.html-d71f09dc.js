import{_ as e,W as o,X as d,a1 as c}from"./framework-bcad4379.js";const t={},i=c('<h2 id="更新已收藏的在线歌单" tabindex="-1"><a class="header-anchor" href="#更新已收藏的在线歌单" aria-hidden="true">#</a> 更新已收藏的在线歌单</h2><p>该功能仅对直接从歌单详情页点“收藏”按钮收藏的歌单有效，可右击已收藏的列表名从弹出的菜单中选择“更新”使用该功能</p><div class="custom-container danger"><p class="custom-container-title">警告</p><p>这将会<strong>覆盖</strong>本地的目标列表，歌曲将被替换成最新的在线列表。</p></div><h2 id="常见问题" tabindex="-1"><a class="header-anchor" href="#常见问题" aria-hidden="true">#</a> 常见问题</h2><p>不支持跨源打开歌单，请<strong>确认</strong>你需要打开的歌单平台是否与软件标签所写的 <strong>歌单源</strong>对应（不一样的话请通过右上角切换歌单源 ）；</p><div class="custom-container info"><p class="custom-container-title">相关信息</p><ul><li><p>小蜗音乐=酷我音乐</p></li><li><p>小枸音乐=酷狗音乐</p></li><li><p>小秋音乐=企鹅音乐</p></li><li><p>小芸音乐=网易音乐</p></li><li><p>小蜜音乐=咪咕音乐</p></li></ul><p>可以去 <code>设置-音源名字</code> 将其修改为原名</p></div><p>对于分享出来的歌单，若打开失败，可尝试先在浏览器中打开后，再从浏览器地址栏复制 URL 地址到软件打开。</p><div class="custom-container tip"><p class="custom-container-title">提示</p><p>网易源的<code>我喜欢</code>歌单无法在未登录的情况下打开，所以你需要在网易端手动创建一个歌单后将<code>我喜欢</code>里的歌曲移动到该歌单打开。</p><p>也可以使用<code>token</code>打开(手机端不支持<code>token</code>打开)。</p></div><h2 id="使用-token-打开网易源我喜欢歌单" tabindex="-1"><a class="header-anchor" href="#使用-token-打开网易源我喜欢歌单" aria-hidden="true">#</a> 使用 token 打开网易源我喜欢歌单</h2><p>由于网易源的“我喜欢”歌单需要登录才能打开，从 v1.13.0 起提供了可以以注入 token 的方式打开网易源“我喜欢”歌单的功能，现若想 要打开此类歌单，需要在歌单链接或 id 后面拼上 <code>###</code> 再加上有效的 token，拼接格式：<code>[id|url]###token</code>，例子（最后面的 xxxxxx 替换成你的 token）：</p><ul><li><code>https://music.163.com/#/playlist?id=11332&amp;userid=123456###xxxxxx</code></li><li><code>11332###xxxxxx</code></li></ul><p>即：将 <code>歌单链接或者歌单ID</code>、<code>###</code>、<code>token</code> 这三者拼到一起。</p><h3 id="token-的获取方法" tabindex="-1"><a class="header-anchor" href="#token-的获取方法" aria-hidden="true">#</a> token 的获取方法</h3><div class="custom-container danger"><p class="custom-container-title">警告</p><p><strong><code>token</code>是你账号的临时身份令牌，不要随便泄露给他人</strong></p></div><p>在浏览器打开登录网易云音乐并<strong>登录</strong>后，按<kbd>F12</kbd>，此时将会打开开发者窗口，然后按你使用的浏览器操作：</p><p><strong>Chrome、Edge、360、QQ 等浏览器</strong></p><p>这些浏览器打开此窗口时界面可能是中文也可能是英文，英文的话按括号里的来</p><ol><li>点击窗口顶部<code>应用程序(application)</code>（若找不到此选项，则可能是被折叠起来了，看看顶部菜单的<code>&gt;&gt;</code>）</li><li>展开左侧 <code>Cookies</code></li><li>点击 <code>https://music.163.com</code></li><li>在右侧窗口找到 <code>名称(Name)</code> 为 <code>MUSIC_U</code> 的这行，这行的第二列（<code>值(Value)</code>）内的那串内容就是<code>token</code>。</li><li>双击它进入编辑状态，然后按<kbd>Ctrl</kbd>+<kbd>C</kbd>键就可以将它复制</li></ol><p><strong>火狐浏览器</strong></p><ol><li>点击窗口顶部<code>存储</code></li><li>展开左侧 <code>Cookie</code></li><li>点击 <code>https://music.163.com</code></li><li>在右侧窗口找到 <code>名称</code> 为 <code>MUSIC_U</code> 的这行，这行的最后一列（<code>值</code>）内的那串内容就是<code>token</code>。</li><li>双击它进入编辑状态，然后按<kbd>Ctrl</kbd>+<kbd>C</kbd>键就可以将它复制</li></ol><h2 id="打开歌单的详细说明" tabindex="-1"><a class="header-anchor" href="#打开歌单的详细说明" aria-hidden="true">#</a> 打开歌单的详细说明...</h2><h3 id="企鹅音乐" tabindex="-1"><a class="header-anchor" href="#企鹅音乐" aria-hidden="true">#</a> 企鹅音乐</h3><p><img src="https://tva4.sinaimg.cn/large/007SWtfrly1h8gl3cvtabj30rs1wg4qp.jpg" alt="TX-V2"></p><h3 id="网易音乐" tabindex="-1"><a class="header-anchor" href="#网易音乐" aria-hidden="true">#</a> 网易音乐</h3><p><img src="https://tvax2.sinaimg.cn/large/007SWtfrly1h8gl7gn311j30rs1h81kx.jpg" alt="WY-V2"></p>',25),a=[i];function n(l,s){return o(),d("div",null,a)}const p=e(t,[["render",n],["__file","open-songlist.html.vue"]]);export{p as default};
